import { StyleSheet } from 'react-native';
import { mainColor, leftMargin, bgColor, bgFrame } from './shared.style';

const chooseLyingSeatStyle = StyleSheet.create({
    mainContent: {
        backgroundColor: 'white',
        paddingRight: 65,
        paddingLeft: 65,
        paddingTop: 10,
        paddingBottom: 20,
        // flexDirection: 'row',
        // justifyContent: 'space-between'
    },
});

export default chooseLyingSeatStyle;